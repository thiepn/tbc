name: Patch rendering repair certification

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/patch-rendering-repair-certification.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/patch_rendering_repair_certification.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/tbc-product-identity.cjs
          git commit -m 'cert: authorize literal-newline rendering repair successor'
          git push
