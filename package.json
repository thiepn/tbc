{
  "name": "the-bible-challenge-preservation",
  "version": "0.0.0",
  "private": true,
  "description": "Read-only recovery and preservation tooling; index.html remains the deployable core.",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/tbc-recovery-stage0.cjs build",
    "test": "node scripts/tbc-recovery-stage0.cjs test",
    "audit:p2a": "node scripts/tbc-recovery-stage0.cjs audit",
    "verify": "node scripts/tbc-recovery-stage0.cjs verify",
    "deploy:verify": "node scripts/tbc-recovery-stage0.cjs deploy-check"
  },
  "devDependencies": { "playwright": "1.55.1" }
}
